.anchor
.DS_Store
CONTEXT.md
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Worker encryption key — contains secret, do not commit
worker/.worker-key

# FHE sidecar — keys are secret, target is build output
fhe-sidecar/.fhe-keys/
fhe-sidecar/target/

# Personal pitch notes — never publish
pitch.txt

# SDK build output — generated by `npm run build`, do not commit
sdk/dist/

# SDK lock file — consumers generate their own
sdk/package-lock.json
